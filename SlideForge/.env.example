# 复制为同目录 `.env` 后，`docker compose` 会用来做「文件内变量替换」。
# 应用密钥、数据库以外的业务项在 `backend/.env`（见 backend/.env.example）。
# 部署细节、Worker 分体、端口与 bind 说明见本目录 `docker-compose.yml` 文件头。

# 换镜像仓库时取消注释并改掉前缀（不设则用 compose 里的 creepender42 默认）：
#SLIDEFORGE_BACKEND_IMAGE=creepender42/barevid-backend:latest
#SLIDEFORGE_FRONTEND_IMAGE=creepender42/barevid-frontend:latest

# 导出 Worker 在别的机器 / 走域名反代时，填「Worker 上浏览器能打开的」地址（默认同机可不改）：
#EXPORT_API_URL=https://你的域名
#EXPORT_FRONTEND_URL=https://你的域名

# 不经过宿主机 nginx、直接暴露容器端口时，可把 bind 改成 0.0.0.0：
#SLIDEFORGE_BACKEND_BIND_HOST=0.0.0.0
#SLIDEFORGE_FRONTEND_BIND_HOST=0.0.0.0
#SLIDEFORGE_MYSQL_BIND_HOST=0.0.0.0
